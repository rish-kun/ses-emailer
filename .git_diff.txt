diff --git a/api/routers/email.py b/api/routers/email.py
index 82ac922..08c6eea 100644
--- a/api/routers/email.py
+++ b/api/routers/email.py
@@ -13,7 +13,7 @@ from email.mime.text import MIMEText
 from pathlib import Path
 from typing import Optional
 
-from fastapi import APIRouter, Depends, File, HTTPException, UploadFile
+from fastapi import APIRouter, Depends, File, Form, HTTPException, UploadFile
 from pydantic import BaseModel
 from sse_starlette.sse import EventSourceResponse
 
@@ -41,10 +41,6 @@ class CompareRequest(BaseModel):
     email_ids: list[str] | None = None
 
 
-class ExcelUploadRequest(BaseModel):
-    column_index: int = 0
-
-
 # ── Email sending (SSE stream) ────────────────────────────────────────
 
 
@@ -57,6 +53,19 @@ async def send_emails(req: SendRequest):
         config_mgr.apply_env_vars()
         cfg = config_mgr.config
 
+        # Validate source_email before sending
+        if not cfg.aws.source_email or "@" not in cfg.aws.source_email:
+            yield {
+                "event": "error",
+                "data": json.dumps(
+                    {
+                        "error": "Invalid source email configuration. "
+                        "Please set a valid 'aws.source_email' with format: name@domain.com"
+                    }
+                ),
+            }
+            return
+
         batch_size = cfg.batch.batch_size
         delay = cfg.batch.delay_seconds
         use_bcc = cfg.batch.use_bcc
@@ -256,7 +265,7 @@ async def send_emails(req: SendRequest):
 
 
 @router.post("/emails/upload-excel", dependencies=[Depends(verify_token)])
-async def upload_excel(file: UploadFile = File(...), column_index: int = 0):
+async def upload_excel(file: UploadFile = File(...), column_index: int = Form(0)):
     """Upload an Excel file and return parsed recipient emails."""
     if not file.filename or not file.filename.endswith((".xlsx", ".xls", ".csv")):
         raise HTTPException(status_code=400, detail="File must be .xlsx, .xls, or .csv")
@@ -273,7 +282,12 @@ async def upload_excel(file: UploadFile = File(...), column_index: int = 0):
         from sending.email_list import scrape_excel_column
 
         emails = scrape_excel_column(str(dest), column_index)
-        return {"file": file.filename, "column_index": column_index, "recipients": emails, "count": len(emails)}
+        return {
+            "file": file.filename,
+            "column_index": column_index,
+            "recipients": emails,
+            "count": len(emails),
+        }
     except Exception as e:
         raise HTTPException(status_code=400, detail=f"Error parsing Excel: {e}")
 
@@ -343,9 +357,7 @@ def _create_message(
             path = Path(attachment_path)
             with open(path, "rb") as f:
                 part = MIMEApplication(f.read(), Name=path.name)
-                part.add_header(
-                    "Content-Disposition", "attachment", filename=path.name
-                )
+                part.add_header("Content-Disposition", "attachment", filename=path.name)
                 msg.attach(part)
         except Exception:
             pass
diff --git a/sending/email_list.py b/sending/email_list.py
index e99d0b2..917fff3 100644
--- a/sending/email_list.py
+++ b/sending/email_list.py
@@ -1,13 +1,13 @@
 import pandas as pd
 
 
-def scrape_excel_column(file_path, column_index=6):
+def scrape_excel_column(file_path, column_index=0):
     """
-    Scrape the 7th column (index 6) from an Excel file and return as a list.
+    Scrape the 1st column (index 0) from an Excel file and return as a list.
 
     Args:
         file_path (str): Path to the Excel file
-        column_index (int): Column index (0-based, default is 6 for 7th column)
+        column_index (int): Column index (0-based, default is 0 for 1st column)
 
     Returns:
         list: Values from the specified column
diff --git a/ts-tui/src/screens/ComposeScreen.tsx b/ts-tui/src/screens/ComposeScreen.tsx
index f4b1fa5..17cf771 100644
--- a/ts-tui/src/screens/ComposeScreen.tsx
+++ b/ts-tui/src/screens/ComposeScreen.tsx
@@ -230,7 +230,7 @@ export function ComposeScreen({ setScreen, goToSend, initialData }: Props) {
         // Field cycling logic within a tab (Tab key only)
         if (key.tab) {
             let fields: string[] = [];
-            if (activeTab === "recipients") fields = ["recipient_input", "excel_column"];
+            if (activeTab === "recipients") fields = ["recipient_input", "excel_column", "selector"];
             if (activeTab === "content") fields = ["subject", "body_type", "body"];
 
             if (fields.length > 0) {
@@ -363,54 +363,62 @@ export function ComposeScreen({ setScreen, goToSend, initialData }: Props) {
                             <Text dimColor>Currently selected: {excelPath || "None"}</Text>
                         </Box>
 
-                        <Box marginTop={1} width={30}>
-                            <Select
-                                options={[
-                                    { label: "Browse for Excel File...", value: "browse" },
-                                    { label: "Load currently selected", value: "load" },
-                                    { label: "Clear all recipients", value: "clear" },
-                                ]}
-                                onChange={(val) => {
-                                    if (val === "browse") setOverlay("filepicker_excel");
-                                    if (val === "load") handleLoadExcel(excelPath, excelColumn);
-                                    if (val === "clear") { setRecipients([]); setMessage("Recipients cleared"); }
-                                }}
-                            />
-                        </Box>
-
-                        {loadingExcel && <Box marginTop={1}><Spinner label="Loading Excel file..." /></Box>}
-
-                        <Box marginTop={1} marginBottom={0}>
-                            <Text bold dimColor>── Test Recipients ──</Text>
-                        </Box>
-
-                        {loadingTestRecipients ? (
-                            <Box marginTop={1}><Spinner label="Loading test recipients..." /></Box>
-                        ) : testRecipients.length === 0 ? (
+                        {activeField === "recipient_input" || activeField === "excel_column" ? (
                             <Box marginTop={1}>
-                                <Text dimColor italic>No test recipients configured. Add them in Config → test_recipients.</Text>
+                                <Text dimColor italic>Press Tab to navigate to Excel Import / Test Recipients options</Text>
                             </Box>
                         ) : (
-                            <Box marginTop={1} flexDirection="column">
-                                <Text dimColor>Space to select, Enter to add selected to recipients:</Text>
-                                <Box marginTop={1}>
-                                    <MultiSelect
-                                        options={testRecipients.map((r) => ({ label: r, value: r }))}
-                                        onSubmit={(selected) => {
-                                            if (selected.length === 0) return;
-                                            const newOnes = selected.filter((r) => !recipients.includes(r));
-                                            if (newOnes.length > 0) {
-                                                setRecipients((prev) => [...prev, ...newOnes]);
-                                                setMessage(`Added ${newOnes.length} test recipient(s)`);
-                                                setMessageType("success");
-                                            } else {
-                                                setMessage("Selected recipients already added");
-                                                setMessageType("error");
-                                            }
+                            <>
+                                <Box marginTop={1} width={30}>
+                                    <Select
+                                        options={[
+                                            { label: "Browse for Excel File...", value: "browse" },
+                                            { label: "Load currently selected", value: "load" },
+                                            { label: "Clear all recipients", value: "clear" },
+                                        ]}
+                                        onChange={(val) => {
+                                            if (val === "browse") setOverlay("filepicker_excel");
+                                            if (val === "load") handleLoadExcel(excelPath, excelColumn);
+                                            if (val === "clear") { setRecipients([]); setMessage("Recipients cleared"); }
                                         }}
                                     />
                                 </Box>
-                            </Box>
+
+                                {loadingExcel && <Box marginTop={1}><Spinner label="Loading Excel file..." /></Box>}
+
+                                <Box marginTop={1} marginBottom={0}>
+                                    <Text bold dimColor>── Test Recipients ──</Text>
+                                </Box>
+
+                                {loadingTestRecipients ? (
+                                    <Box marginTop={1}><Spinner label="Loading test recipients..." /></Box>
+                                ) : testRecipients.length === 0 ? (
+                                    <Box marginTop={1}>
+                                        <Text dimColor italic>No test recipients configured. Add them in Config → test_recipients.</Text>
+                                    </Box>
+                                ) : (
+                                    <Box marginTop={1} flexDirection="column">
+                                        <Text dimColor>Space to select, Enter to add selected to recipients:</Text>
+                                        <Box marginTop={1}>
+                                            <MultiSelect
+                                                options={testRecipients.map((r) => ({ label: r, value: r }))}
+                                                onSubmit={(selected) => {
+                                                    if (selected.length === 0) return;
+                                                    const newOnes = selected.filter((r) => !recipients.includes(r));
+                                                    if (newOnes.length > 0) {
+                                                        setRecipients((prev) => [...prev, ...newOnes]);
+                                                        setMessage(`Added ${newOnes.length} test recipient(s)`);
+                                                        setMessageType("success");
+                                                    } else {
+                                                        setMessage("Selected recipients already added");
+                                                        setMessageType("error");
+                                                    }
+                                                }}
+                                            />
+                                        </Box>
+                                    </Box>
+                                )}
+                            </>
                         )}
                     </Box>
                 </SectionBox>
diff --git a/ts-tui/src/screens/HistoryScreen.tsx b/ts-tui/src/screens/HistoryScreen.tsx
index b511cdc..bc9436c 100644
--- a/ts-tui/src/screens/HistoryScreen.tsx
+++ b/ts-tui/src/screens/HistoryScreen.tsx
@@ -3,6 +3,7 @@
  */
 
 import React, { useState, useEffect, useCallback } from "react";
+import { spawnSync } from "child_process";
 import { Box, Text, useInput } from "ink";
 import { Spinner, Alert } from "@inkjs/ui";
 import { listCampaigns, getStats, getCampaignDetail } from "../api.js";
@@ -25,6 +26,12 @@ interface Campaign {
     email_ids: string[];
 }
 
+/** Copy text to the macOS clipboard via pbcopy. Returns true on success. */
+function copyToClipboard(text: string): boolean {
+    const result = spawnSync("pbcopy", { input: text, encoding: "utf8" });
+    return result.status === 0;
+}
+
 export function HistoryScreen({ setScreen }: Props) {
     const [loading, setLoading] = useState(true);
     const [campaigns, setCampaigns] = useState<Campaign[]>([]);
@@ -35,6 +42,8 @@ export function HistoryScreen({ setScreen }: Props) {
     const [detail, setDetail] = useState<Record<string, unknown> | null>(null);
     const [showDetail, setShowDetail] = useState(false);
     const [error, setError] = useState("");
+    const [copiedSent, setCopiedSent] = useState(false);
+    const [copiedFailed, setCopiedFailed] = useState(false);
 
     const loadData = useCallback(async (query = "") => {
         setLoading(true);
@@ -66,32 +75,66 @@ export function HistoryScreen({ setScreen }: Props) {
         }
     }, []);
 
-    useInput((input, key) => {
-        if (key.escape) {
-            if (showDetail) {
-                setShowDetail(false);
-                setDetail(null);
-            } else if (searchActive) {
-                setSearchActive(false);
-            } else {
-                setScreen("home");
-            }
-            return;
+    const copySentRecipients = useCallback(() => {
+        if (!detail) return;
+        const sent = (detail["sent_records"] as Array<Record<string, unknown>>) || [];
+        const emails = sent.map((r) => r["recipient"] as string).join(", ");
+        if (copyToClipboard(emails)) {
+            setCopiedSent(true);
+            setTimeout(() => setCopiedSent(false), 2000);
+        } else {
+            setError("Failed to copy to clipboard");
         }
+    }, [detail]);
+
+    const copyFailedRecipients = useCallback(() => {
+        if (!detail) return;
+        const failed = (detail["failed_records"] as Array<Record<string, unknown>>) || [];
+        if (failed.length === 0) return;
+        const emails = failed.map((r) => r["recipient"] as string).join(", ");
+        if (copyToClipboard(emails)) {
+            setCopiedFailed(true);
+            setTimeout(() => setCopiedFailed(false), 2000);
+        } else {
+            setError("Failed to copy to clipboard");
+        }
+    }, [detail]);
+
+    // Per Ink docs: useInput accepts { isActive } to disable the handler.
+    // We suppress all input while the search field is active (FormField
+    // owns keyboard input then), and re-enable when it closes.
+    useInput(
+        (input, key) => {
+            if (key.escape) {
+                if (showDetail) {
+                    setShowDetail(false);
+                    setDetail(null);
+                    setCopiedSent(false);
+                    setCopiedFailed(false);
+                } else {
+                    setScreen("home");
+                }
+                return;
+            }
 
-        if (searchActive) return; // TextInput handles keys
+            if (showDetail) {
+                if (input === "c" || input === "C") copySentRecipients();
+                if (input === "f" || input === "F") copyFailedRecipients();
+                return;
+            }
 
-        if (!showDetail) {
+            // List view controls
             if (key.upArrow && selectedIdx > 0) setSelectedIdx((i) => i - 1);
             if (key.downArrow && selectedIdx < campaigns.length - 1)
                 setSelectedIdx((i) => i + 1);
             if (key.return && campaigns[selectedIdx]) {
                 loadDetail(campaigns[selectedIdx].id);
             }
-            if (input === "/" && !searchActive) setSearchActive(true);
-            if (input === "r") loadData(search);
-        }
-    });
+            if (input === "/" ) setSearchActive(true);
+            if (input === "r" || input === "R") loadData(search);
+        },
+        { isActive: !searchActive }
+    );
 
     if (loading) {
         return (
@@ -108,6 +151,12 @@ export function HistoryScreen({ setScreen }: Props) {
         const failed = (detail["failed_records"] as Array<Record<string, unknown>>) || [];
         const sent = (detail["sent_records"] as Array<Record<string, unknown>>) || [];
 
+        const detailHints = [
+            { key: "C", label: "Copy sent" },
+            ...(failed.length > 0 ? [{ key: "F", label: "Copy failed" }] : []),
+            { key: "Esc", label: "Back to list" },
+        ];
+
         return (
             <Box flexDirection="column">
                 <Text bold color="magenta">📋 Campaign Detail</Text>
@@ -150,8 +199,18 @@ export function HistoryScreen({ setScreen }: Props) {
                     </SectionBox>
                 )}
 
-                <Box marginTop={1}>
-                    <KeyHint hints={[{ key: "Esc", label: "Back to list" }]} />
+                <Box marginTop={1} flexDirection="column" gap={0}>
+                    <KeyHint hints={detailHints} />
+                    {copiedSent && (
+                        <Text color="green" bold>
+                            ✓ Copied {sent.length} sent recipient(s) to clipboard!
+                        </Text>
+                    )}
+                    {copiedFailed && (
+                        <Text color="yellow" bold>
+                            ✓ Copied {failed.length} failed recipient(s) to clipboard!
+                        </Text>
+                    )}
                 </Box>
             </Box>
         );
