# Portable dependency list mirroring pyproject.toml.
#
# The primary, supported install path is `uv sync` (uses pyproject.toml +
# uv.lock). This file is provided only for environments without uv:
#   pip install -r requirements.txt
boto3>=1.42.24
pandas>=2.3.3
python-dotenv>=1.2.1
openpyxl>=3.1.0
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
python-multipart>=0.0.18
sse-starlette>=2.0.0
